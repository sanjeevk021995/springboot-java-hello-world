apiVersion: v1
kind: Service
metadata:
  name: hello-service
  namespace: demo
  labels:
    app: hello
spec:
  selector:
    app: hello
  ports:
  - name: http
    port: 80
    targetPort: 8080